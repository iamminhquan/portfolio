/* ── Timeline barrel export ─────────────────────────────────────── */

export { TimelineController } from "./TimelineController";
export { useTimeline, TimelineContext } from "./TimelineContext";
export { CHAPTERS } from "./timeline.config";
export type { Chapter, TimelineState, TimelineAPI } from "./timeline.types";
